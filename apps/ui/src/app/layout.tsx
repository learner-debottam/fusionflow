import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'FusionFlow - Next-Gen Middleware Integration Platform',
  description: 'A modern, scalable middleware integration platform for seamless data flow and process automation.',
  keywords: ['middleware', 'integration', 'workflow', 'automation', 'api'],
  authors: [{ name: 'FusionFlow Team' }],
  viewport: 'width=device-width, initial-scale=1',
  robots: 'index, follow',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-background">
          {children}
        </div>
      </body>
    </html>
  )
}
